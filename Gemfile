source 'https://rubygems.org'

gem 'sinatra'
gem 'sinatra-contrib'
gem 'thin'

gem 'slim'
gem 'sprockets', '~> 3'

gem 'sequel'
gem 'sinatra-sequel'

gem 'pg'
gem 'sequel_pg', require: 'sequel'

gem 'rest-client'

# Password hashing
gem 'argon2'

# Needed for /mail routes
gem 'mail'
gem 'maildir'
gem 'sanitize'

group :production do
  gem 'uglifier'
  gem 'sass'
end

group :development do
  # Debugging
  gem 'pry'
end
